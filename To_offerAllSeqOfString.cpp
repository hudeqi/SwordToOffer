#include<iostream>
#include<string>
#include<vector>
using namespace std;

//vector<string> Permutation(string str) {
//	if (str.size() == 0)
//		return vector<string>();
//	if (str.size() == 1)
//		return vector<string>(1, str);
//
//}